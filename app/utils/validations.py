class Validation:
    @staticmethod
    def validate(data):
        # # Validate structure
        # if "flashcards" not in data or "multiple_choice_questions" not in data:
        #     raise ValueError("Missing required fields in AI response")